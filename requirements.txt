# ============================================
# INDMoney Weekly Pulse — Python Dependencies
# ============================================

# Phase 1 — Config
python-dotenv==1.0.0

# Phase 2 — Review Scraping
google-play-scraper==1.2.0
python-dateutil>=2.8.2
emoji==2.0.0
langdetect==1.0.9

# Phase 4 — Theme Generation (Groq)
groq==0.4.0

# Phase 5 & 6 — Pulse & Email (Gemini 2.5 Flash)
google-genai==1.0.0

# Phase 6 — Email Templating + UI
Jinja2==3.1.3
streamlit==1.32.2
plotly==5.19.0
pandas==2.2.1

# Phase 10B - Model Context Protocol SDK
mcp>=1.0.0
